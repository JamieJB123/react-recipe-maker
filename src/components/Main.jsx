export default function Main() {
    return (
        <main>
            <form className="ingredient-form">
                <input placeholder="e.g. oregano" aria-label="Add ingredient" type="text"></input>
                <button>+ Add ingredient</button>
            </form>
        </main>
    )
}
